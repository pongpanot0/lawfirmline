# Completion: Drag-and-drop uploads

**Date:** 2026-09-10  
**Request:** Add drag-and-drop on pages that upload images/files.

## Changes

- Extended `DocumentDropZone` with `multiple` + `onFiles`, replaced emoji with Upload icon.
- **Expenses new** (`/expenses/new`) — receipt image upload now drag-and-drop.
- **Intake detail** (`/intake/[id]`) — PDF upload zone with drag-and-drop (multi).
- **BatchAnalysisPanel** (case / new intake / new case) — multi-file drag-and-drop.

Already had drop zones: documents hub, case documents, document version review, portal intake new.
