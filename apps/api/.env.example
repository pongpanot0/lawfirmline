DATABASE_URL="postgresql://lawfirm:lawfirm@localhost:5433/lawfirm"
JWT_SECRET="dev-jwt-secret-change-in-production"
JWT_REFRESH_SECRET="dev-refresh-secret-change-in-production"
CLIENT_PORTAL_JWT_SECRET="dev-client-portal-secret-change-in-production"
JWT_EXPIRES_IN="15m"
JWT_REFRESH_EXPIRES_IN="7d"
UPLOAD_DIR="./uploads"
PORT=3001
# Comma-separated allowed frontend origins (required for production)
CORS_ORIGIN="http://localhost:3000"
HOST="0.0.0.0"

# Railway: link Postgres plugin and use its DATABASE_URL
# DATABASE_URL="${{Postgres.DATABASE_URL}}"
# CORS_ORIGIN="https://your-web-app.up.railway.app"

# Optional integrations
OPENAI_API_KEY=""
GOOGLE_MAPS_API_KEY=""

# SendGrid (team invitations)
SENDGRID_API_KEY=""
SENDGRID_FROM_EMAIL=""
SENDGRID_FROM_NAME="LexFlow"
SENDGRID_SKIP_TLS_VERIFY="true"
APP_URL=""

# LINE Messaging API (Official Account)
LINE_CHANNEL_ID=""
LINE_CHANNEL_SECRET=""
# Optional: long-lived channel access token from LINE Developers Console
LINE_CHANNEL_ACCESS_TOKEN=""
# Optional: comma-separated LINE user IDs for push delivery
LINE_PUSH_USER_IDS=""

# Omise payment gateway (Thailand) — get keys from https://dashboard.omise.co
OMISE_PUBLIC_KEY="pkey_test_xxxxxxxx"
OMISE_SECRET_KEY="skey_test_xxxxxxxx"
OMISE_MOCK="false"
# Local dev only — fixes UNABLE_TO_GET_ISSUER_CERT_LOCALLY on some Mac/corp networks
OMISE_SKIP_TLS_VERIFY="true"
# Webhook URL (Omise Dashboard → Webhooks): POST https://your-api/saas/webhooks/omise
# Events: charge.complete
