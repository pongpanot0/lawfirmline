/* Refine the existing narrative-first research workspace. No AI requests or credit use. */
let researchMode='ข้อเท็จจริงและฎีกา',researchNarrative='',researchQuestion='',researchPreview='',researchSelected=new Set();
const researchFacts=[];
function researchDocs(){return `<section class="research-docs"><div class="block-title"><h3>เอกสารที่ใช้ · เลือก ${researchSelected.size} ไฟล์</h3><button class="link" onclick="activeTab='เอกสาร';render()">เพิ่ม / จัดการเอกสาร ↗</button></div>${uploadedFiles.length?uploadedFiles.map((f,i)=>`<label class="quiet-check"><input type="checkbox" ${researchSelected.has(i)?'checked':''} onchange="this.checked?researchSelected.add(${i}):researchSelected.delete(${i});render()"><span>${esc(f.name)}<small class="muted"> · ตัวอย่างชื่อไฟล์ ยังไม่ได้อ่านข้อความ</small></span></label>`).join(''):'<p class="muted small">ยังไม่มีเอกสารในตัวอย่าง เพิ่มเอกสารจากแท็บเอกสารเพื่อเลือกใช้</p>'}</section>`}
function researchRun(kind){researchPreview=kind;render()}
function researchPreviewPanel(){if(!researchPreview)return '';return `<section class="original-section" aria-label="ผลการทำงาน"><div class="block-title"><h2>${esc(researchPreview)}</h2><span class="pill">ตัวอย่างหน้าผลลัพธ์</span></div><p class="small">ยังไม่ได้เรียก AI หรือค้นฐานฎีกาจริง จึงยังไม่มีคำตอบหรือคำพิพากษาให้ยืนยัน</p>${viewValues([['ข้อมูลที่ใช้',researchPreview==='สรุปข้อมูลจากเอกสาร'?'เฉพาะเอกสารที่เลือก':researchNarrative||researchQuestion],['เอกสารต้นทาง',[...researchSelected].map(i=>uploadedFiles[i]?.name).filter(Boolean).join(', ')||'ไม่ได้เลือก'],['แหล่งอ้างอิง','จะแสดงชื่อเอกสารและเลขหน้า หรือเลขฎีกาพร้อมแหล่งฉบับเต็ม'],['การตรวจ','ตรวจข้อเท็จจริงและความเกี่ยวข้องก่อนนำไปใช้']])}<button class="link" onclick="researchPreview='';render()">ปิดตัวอย่างผล</button></section>`}
function factEditor(index=-1){const r=researchFacts[index]||{};const m=document.getElementById('modal');m.classList.add('wide');m.innerHTML=`<h2>${index<0?'เพิ่ม':'แก้ไข'}ข้อเท็จจริง</h2><p class="data-note">ข้อมูลที่ทนายบันทึกเอง ไม่ใช่ข้อสรุปจาก AI</p><form onsubmit="event.preventDefault();saveResearchFact(this,${index})"><div class="fields"><div class="field full"><label for="fact-text">ข้อเท็จจริง *</label><textarea id="fact-text" name="text" required>${esc(r.text||'')}</textarea></div><div class="field"><label for="fact-source">เอกสาร / แหล่งอ้างอิง *</label><input id="fact-source" name="source" value="${esc(r.source||'')}" required></div><div class="field"><label for="fact-page">หน้า / ตำแหน่ง *</label><input id="fact-page" name="page" value="${esc(r.page||'')}" required></div></div><div class="modal-actions"><button type="button" class="btn" onclick="document.getElementById('modal').close()">ยกเลิก</button><button class="btn primary" type="submit">บันทึกข้อเท็จจริง</button></div></form>`;m.showModal()}
function saveResearchFact(form,index){const row=Object.fromEntries(new FormData(form));if(Object.values(row).some(v=>!v.trim()))return;row.reviewed=false;if(index<0)researchFacts.push(row);else researchFacts[index]=row;document.getElementById('modal').close();render()}
function factSource(index){const r=researchFacts[index];const m=document.getElementById('modal');m.innerHTML=`<h2>แหล่งอ้างอิงที่บันทึก</h2>${viewValues([['ข้อเท็จจริง',r.text],['เอกสาร',r.source],['หน้า / ตำแหน่ง',r.page]])}<p class="data-note">ต้นฉบับยังไม่เชื่อมต่อใน mockup กรุณาเปิดเอกสารจริงเพื่อตรวจเทียบ</p><div class="modal-actions"><button class="btn" onclick="document.getElementById('modal').close()">ปิด</button></div>`;m.showModal()}
function reviewedFacts(){return `<section class="original-section"><div class="block-title"><h2>ข้อเท็จจริงในคดี</h2><button class="btn" onclick="factEditor()">+ เพิ่มข้อเท็จจริง</button></div>${['รอตรวจ','ตรวจแล้ว'].map((label,n)=>`<h3 style="margin-top:24px">${label} · ${researchFacts.filter(r=>r.reviewed===!!n).length}</h3>${researchFacts.map((r,i)=>r.reviewed===!!n?`<article class="record-row"><p>${esc(r.text)}</p><p class="muted small">${esc(r.source)} · ${esc(r.page)}</p><div class="extra-actions"><button class="link" onclick="factEditor(${i})">แก้ไข</button><button class="link" onclick="factSource(${i})">ดูแหล่งอ้างอิง</button>${!r.reviewed?`<button class="link" onclick="researchFacts[${i}].reviewed=true;render()">ตรวจเทียบต้นฉบับแล้ว</button>`:'<span class="muted small">ทนายระบุว่าตรวจแล้ว · แก้ไขจะกลับไปรอตรวจ</span>'}</div></article>`:'').join('')}`).join('')}${!researchFacts.length?'<p class="muted small" style="margin-top:16px">ยังไม่มีข้อเท็จจริง เพิ่มบันทึกเอง หรือรอผลวิเคราะห์จากเอกสาร</p>':''}</section>`}
const legacyResearch=aiPanel;
aiPanel=function(){return `<div class="refined-stack research-refined"><div class="data-toolbar" role="group" aria-label="เครื่องมือข้อเท็จจริง">${['ข้อเท็จจริงและฎีกา','ถามเอกสาร','สรุปเอกสารเดิม'].map(mode=>`<button class="btn ${researchMode===mode?'primary':''}" aria-pressed="${researchMode===mode}" onclick="researchMode='${mode}';researchPreview='';render()">${mode}</button>`).join('')}</div>${researchMode==='ข้อเท็จจริงและฎีกา'?`<section class="original-section"><h2>เริ่มจากเรื่องที่ต้องการรู้</h2><p class="muted small">ระบุเหตุการณ์หรือประเด็น แล้วเลือกวิธีทำงาน</p><div class="field" style="margin-top:16px"><label for="research-narrative">เหตุการณ์หรือประเด็นที่ต้องการค้น</label><textarea id="research-narrative" oninput="researchNarrative=this.value;refreshResearchButtons()" placeholder="สรุปเรื่อง ข้อโต้แย้ง และสิ่งที่ต้องการหาคำตอบ">${esc(researchNarrative)}</textarea></div>${researchDocs()}<div class="research-actions"><div><button id="research-precedent" class="btn primary" ${researchNarrative.trim()?'':'disabled'} onclick="researchRun('ค้นฎีกา')">ค้นฎีกา</button><p class="helper">ใช้ประเด็นที่พิมพ์ เพื่อหาแนวคำพิพากษาที่เกี่ยวข้อง</p></div><div><button id="research-organize" class="btn" ${researchNarrative.trim()?'':'disabled'} onclick="researchRun('จัดข้อเท็จจริง')">จัดข้อเท็จจริง</button><p class="helper">ใช้ข้อความที่พิมพ์ ข้อมูลยังต้องตรวจเทียบหลักฐาน</p></div><div><button class="btn" ${researchSelected.size?'':'disabled'} onclick="researchRun('สรุปข้อมูลจากเอกสาร')">สรุปข้อมูลจากเอกสาร</button><p class="helper">ใช้เฉพาะไฟล์ที่เลือก ไม่รวมข้อความด้านบน</p></div></div><p class="data-note">ปุ่มใน mockup เปิดตัวอย่างหน้าผลลัพธ์ ไม่ใช้เครดิตหรือส่งข้อมูลให้ AI</p></section>${researchPreviewPanel()}${reviewedFacts()}`:researchMode==='ถามเอกสาร'?`<section class="original-section"><h2>ถามจากสำนวนคดี</h2>${researchDocs()}<div class="field"><label for="research-question">คำถามจากเอกสาร</label><textarea id="research-question" oninput="researchQuestion=this.value;refreshResearchButtons()">${esc(researchQuestion)}</textarea></div><button id="research-ask" class="btn primary" style="margin-top:16px" ${researchSelected.size&&researchQuestion.trim()?'':'disabled'} onclick="researchRun('คำตอบจากเอกสาร')">ถามเอกสาร</button><p class="helper">เลือกเอกสารและระบุคำถามก่อน · ไม่เรียก AI จริงในตัวอย่าง</p></section>${researchPreviewPanel()}`:`${reviewedFacts()}${fold('ข้อมูลสรุปและบันทึกเดิม',legacyResearch())}`}</div>`}
function refreshResearchButtons(){const p=document.getElementById('research-precedent'),o=document.getElementById('research-organize'),a=document.getElementById('research-ask');if(p)p.disabled=!researchNarrative.trim();if(o)o.disabled=!researchNarrative.trim();if(a)a.disabled=!researchSelected.size||!researchQuestion.trim()}
render();
